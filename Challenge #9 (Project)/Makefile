# Makefile for cocotb testing of lfsr16.v

# Tell cocotb we’re using Verilog
TOPLEVEL_LANG = verilog

# List your RTL sources here
VERILOG_SOURCES = lfsr16.v

# The top-level module name
TOPLEVEL = lfsr16

# The Python module (without .py) in tests/
MODULE = test_lfsr

# Optional: shorter log messages
COCOTB_REDUCED_LOG_FMT = 1
